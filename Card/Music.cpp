#include <Windows.h>
#include <mmsystem.h>
#include <conio.h>
#include <cstring>
#pragma comment(lib,"winmm.lib")
/*
char option[5][10] = { "open ", "play ", "pause ", "stop ",  };
char Path[2][30] = { "S:\\PokerMusic\\Name.mp3","S:\\PokerMusic\\EFX.wav" };

void MusicInit() {
	mciSendString("open \"S:\\PMusic\\Name.mp3\" alias something", NULL, 0, NULL);
	mciSendString("open \"S:\\PMusic\\EFX.mp3\" alias something", NULL, 0, NULL);
}

void Play(int plag) {
	mciSendString(strcat(option[0], Path[plag]), NULL, 0, NULL);
}

void Pause(int plag) {
	mciSendString(strcat(option[1], Path[plag]), NULL, 0, NULL);
}

void Stop(int plag) {
	mciSendString(strcat(option[2], Path[plag]), NULL, 0, NULL);
}
*/